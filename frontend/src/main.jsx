import React from "react";
import ReactDOM from "react-dom/client";
import EHRQueryApp from "./EHRQueryApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EHRQueryApp />
  </React.StrictMode>
);
